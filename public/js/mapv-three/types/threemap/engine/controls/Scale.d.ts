/**
 * scale
 *
 * ```javascript
 * const scale = new Scale(contorlContainer, enable, engine);
 * ```
 */
export class Scale extends BaseControl {
    _text: HTMLDivElement | undefined;
    /** @private */
    private calculateScale;
}
import { BaseControl } from "./BaseControl";
