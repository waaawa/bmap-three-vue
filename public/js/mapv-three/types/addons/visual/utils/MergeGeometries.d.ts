/**
 * 合并几何
 * @param {Array} geometries 几何数组
 * @param {boolean} isfat 是否是宽线
 * @returns {Object} 返回合并后的bufferGeometry
 */
export function mergeBufferGeometries(geometries: any[], isfat: boolean): Object;
/**
 * 合并属性
 * @param {Array} attributes 属性数组
 * @returns {Object} 合并后的BufferAttributeA
 */
export function mergeBufferAttributes(attributes: any[]): Object;
