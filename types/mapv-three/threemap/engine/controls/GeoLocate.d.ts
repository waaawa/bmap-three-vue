/**
 * geoLocate
 *
 * ```javascript
 * const geoLocate = new GeoLocate(contorlContainer, enable, engine);
 * ```
 */
export class GeoLocate extends BaseControl {
    locate(): void;
}
import { BaseControl } from "./BaseControl";
